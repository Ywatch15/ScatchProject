<%- include('./partials/header') %>

<div class="relative overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-b from-white via-white to-slate-100"></div>
    <div class="absolute -top-32 right-0 w-72 h-72 bg-brand/10 blur-3xl rounded-full"></div>

    <div class="relative max-w-6xl mx-auto px-6 py-16 space-y-16">
        <% if(error && error.length>0){ %>
            <div class="mb-6 rounded-xl border border-rose-200 bg-rose-50 px-4 py-3 text-rose-700 flex items-center gap-2">
                <i class="ri-error-warning-line"></i>
                <span><%= error %></span>
            </div>
        <% } %>

        <% if(success && success.length>0){ %>
            <div class="mb-6 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-emerald-700 flex items-center gap-2">
                <i class="ri-check-line"></i>
                <span><%= success %></span>
            </div>
        <% } %>

        <div class="grid gap-12 lg:grid-cols-2 items-center">
            <div class="space-y-6">
                <p class="inline-flex items-center gap-2 rounded-full bg-white border border-slate-200 px-4 py-1 text-xs uppercase tracking-[0.2em] text-slate-500">
                    <span class="w-2 h-2 rounded-full bg-emerald-400"></span>
                    Seamless shopping starts here
                </p>
                <h1 class="text-4xl md:text-5xl font-semibold leading-tight text-slate-900">
                    Manage products, carts, and orders inside a single elegant workspace.
                </h1>
                <p class="text-base text-slate-600">
                    Scatch helps you discover curated products, manage your cart with ease, and keep track of orders without the clutter.
                </p>
                <div class="grid grid-cols-3 gap-6">
                    <div>
                        <p class="text-3xl font-semibold text-slate-900">2K+</p>
                        <p class="text-xs uppercase tracking-wide text-slate-500">Daily shoppers</p>
                    </div>
                    <div>
                        <p class="text-3xl font-semibold text-slate-900">48</p>
                        <p class="text-xs uppercase tracking-wide text-slate-500">Curated brands</p>
                    </div>
                    <div>
                        <p class="text-3xl font-semibold text-slate-900">4.9/5</p>
                        <p class="text-xs uppercase tracking-wide text-slate-500">Customer rating</p>
                    </div>
                </div>
            </div>
            <div class="space-y-6">
                <div class="rounded-2xl border border-slate-200 bg-white/90 shadow-lg shadow-slate-200/60 p-6">
                    <h3 class="text-xl font-semibold mb-4 text-slate-900">Create your account</h3>
                    <form class="space-y-4" autocomplete="off" action="/users/register" method="post">
                        <input class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-2.5 focus:border-brand focus:ring-1 focus:ring-brand" type="text" placeholder="Full Name" name="fullname">
                        <input class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-2.5 focus:border-brand focus:ring-1 focus:ring-brand" type="email" placeholder="Email" name="email">
                        <input class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-2.5 focus:border-brand focus:ring-1 focus:ring-brand" type="password" placeholder="Password" name="password">
                        <button class="w-full rounded-xl bg-brand text-white py-2.5 font-medium transition hover:bg-brandDark" type="submit">Create account</button>
                    </form>
                </div>
                <div class="rounded-2xl border border-slate-200 bg-white/80 shadow p-6">
                    <h3 class="text-xl font-semibold mb-4 text-slate-900">Login</h3>
                    <form class="space-y-4" autocomplete="off" action="/users/login" method="post">
                        <input class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-2.5 focus:border-brand focus:ring-1 focus:ring-brand" type="email" placeholder="Email" name="email">
                        <input class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-2.5 focus:border-brand focus:ring-1 focus:ring-brand" type="password" placeholder="Password" name="password">
                        <button class="w-full rounded-xl border border-slate-200 py-2.5 font-medium text-slate-700 hover:border-brand hover:text-brand" type="submit">Continue</button>
                    </form>
                </div>
            </div>
        </div>

        <div class="rounded-3xl border border-slate-200 bg-white/80 p-8 shadow-sm">
            <p class="text-sm font-semibold text-brand mb-2">Why Scatch?</p>
            <div class="grid md:grid-cols-3 gap-6 text-sm text-slate-600">
                <div class="p-5 rounded-2xl border border-slate-100 bg-slate-50">
                    <p class="font-semibold text-slate-900 mb-1">Curated catalog</p>
                    <p>Owners can upload bespoke products that instantly become available in the shop.</p>
                </div>
                <div class="p-5 rounded-2xl border border-slate-100 bg-slate-50">
                    <p class="font-semibold text-slate-900 mb-1">One-tap cart management</p>
                    <p>Users can add, increment, or decrement items with immediate feedback.</p>
                </div>
                <div class="p-5 rounded-2xl border border-slate-100 bg-slate-50">
                    <p class="font-semibold text-slate-900 mb-1">Secure sessions</p>
                    <p>JWT-backed authentication keeps every page protected after you sign in.</p>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('./partials/footer') %>